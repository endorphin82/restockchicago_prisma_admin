mutation DeleteOneCategory($where: CategoryWhereUniqueInput!) {
    deleteOneCategory(where: $where){
        id
        name
        description
        url
        parent
        icon
        img
    }
}
