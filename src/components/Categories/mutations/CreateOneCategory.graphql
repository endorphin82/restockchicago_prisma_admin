mutation CreateOneCategory($data: CategoryCreateInput! ) {
    createOneCategory(data: $data ){
        id
        name
        description
        url
        parent
        icon
        img
        products{
            id
            name
            price
            icon
            img
        }
    }
}
