mutation UpdateOneCategory($data: CategoryUpdateInput!,
$where: CategoryWhereUniqueInput! ) {
    updateOneCategory(data: $data, where: $where){
        id
        name
        description
        url
        parent
        icon
        img
        products{
            id
            name
            price
            icon
            img
        }
    }
}
